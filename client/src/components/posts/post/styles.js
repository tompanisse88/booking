import { makeStyles } from "@mui/styles";

export default makeStyles({
    box: {
      width: "auto",
      borderRadius: "10px",
      backgroundColor: "#808080",
      color: "white",
      textAlign: 'center',
    },
    bookedUser: {
      textAlign: 'left',
    },
    deskid: {
      fontStyle: "bold",
    },
  });