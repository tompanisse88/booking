import { makeStyles } from "@mui/styles";

export default makeStyles({
    box: {
      width: "auto",
      borderRadius: "10px",
      backgroundColor: "rgba(0,0,0,0)",
      color: "white",
      textAlign: 'left',
    }
  });